{
  "name": "@baicie/napi",
  "version": "0.1.0",
  "type": "module",
  "keywords": [],
  "license": "MIT",
  "files": [
    "bin",
    "cli",
    "dist",
    "!dist/*.node"
  ],
  "main": "./dist/index.mjs",
  "module": "./dist/index.mjs",
  "types": "./dist/index.d.mts",
  "exports": {
    ".": {
      "dev": "./src/index.ts",
      "default": "./dist/index.mjs"
    }
  },
  "scripts": {
    "# Scrips for binding #": "_",
    "artifacts": "napi artifacts --cwd ./src --package-json-path ../package.json -o=../artifacts --npm-dir ../npm",
    "build-binding": "oxnode ./build-binding.ts",
    "build-binding:release": "pnpm build-binding --release",
    "build-binding:profile": "pnpm build-binding --profile profile",
    "build-binding:wasi": "pnpm build-binding --target wasm32-wasip1-threads",
    "build-binding:wasi:release": "pnpm build-binding --profile release-wasi --target wasm32-wasip1-threads",
    "# Scrips for node #": "_",
    "build-node": "oxnode -C dev ./build.ts",
    "build-js-glue": "run-p -s build-node",
    "build-native:debug": "run-p -s build-binding build-js-glue",
    "build-native:release": "run-p -s build-binding:release build-js-glue",
    "build-native:profile": "run-p -s build-binding:profile build-js-glue",
    "build-native:memory-profile": "run-p -s build-binding:profile --features default_global_allocator build-js-glue",
    "build-wasi:debug": "TARGET='napi-wasi' run-p -s build-binding build-binding:wasi build-node",
    "build-wasi:release": "TARGET='publish-to-npm-wasi' run-p -s build-binding build-binding:wasi:release build-node",
    "build-browser-pkg:debug": "TARGET='browser' run-p -s build-binding build-binding:wasi build-node",
    "build-browser-pkg:release": "TARGET='browser' run-p -s build-binding build-binding:wasi:release build-node",
    "# Scrips for docs #": "_",
    "prepublishOnly": "napi pre-publish -t npm --no-gh-release"
  },
  "engines": {
    "node": "^20.19.0 || >=22.12.0"
  },
  "devDependencies": {
    "@napi-rs/cli": "^3.5.0",
    "@oxc-node/cli": "^0.0.35"
  },
  "publishConfig": {
    "registry": "https://registry.npmjs.org/",
    "access": "public"
  },
  "napi": {
    "binaryName": "baicie-binding",
    "packageName": "@baicie/napi",
    "targets": [
      "x86_64-apple-darwin",
      "x86_64-pc-windows-msvc",
      "x86_64-unknown-linux-gnu",
      "x86_64-unknown-linux-musl",
      "x86_64-unknown-freebsd",
      "armv7-unknown-linux-gnueabihf",
      "aarch64-unknown-linux-gnu",
      "aarch64-apple-darwin",
      "aarch64-unknown-linux-musl",
      "aarch64-unknown-linux-ohos",
      "aarch64-pc-windows-msvc",
      "aarch64-linux-android",
      "wasm32-wasip1-threads"
    ],
    "wasm": {
      "initialMemory": 16384,
      "browser": {
        "fs": true,
        "asyncInit": true
      }
    }
  }
}
